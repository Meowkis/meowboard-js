PORT=3144
# Set false only when running locally over plain HTTP.
COOKIE_SECURE=true
# Leave empty for a direct connection. Compose defaults to one Caddy hop.
TRUST_PROXY=
ADMIN_PASSWORD_HASH='paste-generated-scrypt-hash-here'
